# Let's Encrypt ACME Configuration
ACME_EMAIL=dev@innexar.app

# Cloudflare Configuration (for DNS automation)
CLOUDFLARE_EMAIL=your-cloudflare-email@example.com
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token

# Traefik Basic Auth (generate with: htpasswd -nb admin <password>)
# TRAEFIK_BASIC_AUTH=admin:$2y$10$... # Replace with actual htpasswd output

# SMTP Configuration (for GitLab notifications)
SMTP_ADDRESS=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_DOMAIN=innexar.app

# GitLab Email Configuration
GITLAB_EMAIL_FROM=gitlab@innexar.app
GITLAB_EMAIL_REPLY_TO=gitlab@innexar.app

# SonarQube Database Configuration
SONAR_DB_USER=sonarqube
SONAR_DB_PASSWORD=change-this-strong-password

# Keycloak Configuration
KEYCLOAK_ADMIN_USER=admin
KEYCLOAK_ADMIN_PASSWORD=change-this-strong-password
KEYCLOAK_DB_USER=keycloak
KEYCLOAK_DB_PASSWORD=change-this-strong-password
